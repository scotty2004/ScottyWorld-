"use client";
import {useEffect,useState}from"react";
export default function Community(){const[rows,setRows]=useState<any[]>([]);const update=async(id:string,status:string)=>{await fetch("/api/admin/community",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({id,status})});setRows(x=>x.map(r=>r.id===id?{...r,status}:r))};useEffect(()=>{fetch("/api/admin/community").then(r=>r.ok?r.json():[]).then(setRows)},[]);
return <div className="space-y-5"><div><h1 className="text-2xl font-bold">Community moderation</h1><p className="text-sm text-muted-foreground">Review user reports and record moderation decisions.</p></div><div className="grid gap-3">{rows.map(r=><div key={r.id} className="rounded-2xl border bg-card p-4"><div className="font-semibold">{r.post?.title||"Reported post"}</div><div className="mt-1 text-xs text-muted-foreground">Reporter: {r.reporter?.username||"Unknown"} · Reason: {r.reason||"—"}</div><div className="mt-3"><select value={r.status} onChange={e=>update(r.id,e.target.value)} className="rounded-lg border bg-background px-2 py-1 text-sm"><option>OPEN</option><option>REVIEWING</option><option>RESOLVED</option><option>DISMISSED</option></select></div></div>)}</div></div>}
